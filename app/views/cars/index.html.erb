<p style="color: green"></p>

<h1>Cars</h1>
  <% if current_user&.admin? %>
    <%= link_to "New car", new_car_path %>
  <% end %>
  <% @cars.each do |car| %>
    <%= render car %>
    <p>
      <%= link_to "Show this car", car %>
    </p>
  <% end %>
